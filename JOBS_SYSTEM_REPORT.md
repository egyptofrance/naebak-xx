# تقرير شامل: نظام الوظائف في منصة نائبك

**التاريخ**: 2 نوفمبر 2025  
**المشروع**: naebak-xx  
**الحالة**: ✅ نظام متكامل وجاهز للاستخدام

---

## 📊 ملخص تنفيذي

نظام الوظائف في منصة نائبك هو **نظام متكامل** لإدارة الوظائف والتقديمات، يشمل:

- ✅ **واجهة عامة** لعرض الوظائف والتقديم عليها
- ✅ **لوحة تحكم إدارية** لإدارة الوظائف والتقديمات
- ✅ **نظام تصفية وبحث** متقدم
- ✅ **نماذج تقديم** شاملة مع رفع الملفات
- ✅ **إحصائيات** للوظائف (المشاهدات والتقديمات)

---

## 🗂️ بنية النظام

### 1. قاعدة البيانات (Database Schema)

#### جدول `jobs` - الوظائف
```typescript
interface Job {
  id: string;
  title: string;                    // عنوان الوظيفة
  description: string;               // الوصف
  
  // معلومات الشركة
  company_name: string | null;
  contact_person: string | null;
  contact_phone: string | null;
  contact_email: string | null;
  posted_by: string | null;
  
  // تفاصيل الوظيفة
  salary_min: number | null;
  salary_max: number | null;
  salary_currency: string;           // EGP, USD, etc.
  work_location: WorkLocation;       // office, remote, hybrid
  office_address: string | null;
  work_hours: string | null;
  employment_type: EmploymentType;   // full-time, part-time, etc.
  category: JobCategory;             // data-entry, management, etc.
  governorate: string | null;        // المحافظة
  
  // متطلبات ومسؤوليات
  requirements: string[] | null;     // المتطلبات
  responsibilities: string[] | null; // المسؤوليات
  benefits: string[] | null;         // المزايا
  
  // إضافات
  image_url: string | null;          // صورة الوظيفة
  status: JobStatus;                 // active, closed, draft
  positions_available: number;       // عدد الوظائف المتاحة
  
  // التواريخ
  created_at: string;
  updated_at: string;
  created_by: string | null;
  updated_by: string | null;
}
```

#### جدول `job_applications` - التقديمات
```typescript
interface JobApplication {
  id: string;
  job_id: string;
  
  // بيانات المتقدم
  full_name: string;
  email: string;
  phone: string;
  national_id: string | null;
  date_of_birth: string | null;
  governorate: string | null;
  city: string | null;
  address: string | null;
  
  // المؤهلات والخبرة
  education_level: EducationLevel | null;  // high-school, bachelor, etc.
  education_details: string | null;
  years_of_experience: number | null;
  previous_experience: string | null;
  skills: string[] | null;
  
  // المرفقات
  cv_url: string | null;                   // رابط السيرة الذاتية
  cover_letter: string | null;             // خطاب التقديم
  portfolio_url: string | null;            // رابط الأعمال
  additional_documents: string[] | null;   // مستندات إضافية
  
  // حالة الطلب
  status: ApplicationStatus;               // pending, reviewing, etc.
  admin_notes: string | null;
  reviewed_by: string | null;
  reviewed_at: string | null;
  
  // التواريخ
  created_at: string;
  updated_at: string;
}
```

#### جدول `job_statistics` - الإحصائيات
```typescript
interface JobStatistics {
  id: string;
  job_id: string;
  views_count: number;              // عدد المشاهدات
  applications_count: number;       // عدد التقديمات
  last_viewed_at: string | null;
  created_at: string;
  updated_at: string;
}
```

---

## 🎨 الواجهات والصفحات

### أ. الواجهة العامة (Public Pages)

#### 1. صفحة قائمة الوظائف `/jobs`
**المسار**: `src/app/[locale]/(external-pages)/jobs/page.tsx`

**الميزات**:
- ✅ عرض جميع الوظائف النشطة
- ✅ نظام تصفية متقدم:
  - التصنيف (Category)
  - نوع العمل (Work Location)
  - نوع التوظيف (Employment Type)
  - المحافظة (Governorate)
  - البحث النصي (Search)
- ✅ ترقيم الصفحات (Pagination)
- ✅ عرض شبكي (Grid) للوظائف
- ✅ عرض الإحصائيات (المشاهدات والتقديمات)

**المكونات**:
- `JobsGrid.tsx` - شبكة عرض الوظائف مع الفلاتر

#### 2. صفحة تفاصيل الوظيفة `/jobs/[id]`
**المسار**: `src/app/[locale]/(external-pages)/jobs/[id]/page.tsx`

**الميزات**:
- ✅ عرض تفاصيل الوظيفة الكاملة
- ✅ صورة الوظيفة (إن وجدت)
- ✅ معلومات الراتب والموقع
- ✅ المتطلبات والمسؤوليات والمزايا
- ✅ معلومات التواصل
- ✅ زر "قدم الآن"
- ✅ عرض الإحصائيات

#### 3. صفحة التقديم على الوظيفة `/jobs/[id]/apply`
**المسار**: `src/app/[locale]/(external-pages)/jobs/[id]/apply/page.tsx`

**الميزات**:
- ✅ نموذج تقديم شامل
- ✅ حقول البيانات الشخصية
- ✅ حقول المؤهلات والخبرة
- ✅ رفع السيرة الذاتية (CV)
- ✅ خطاب التقديم
- ✅ رابط الأعمال (Portfolio)
- ✅ مستندات إضافية
- ✅ التحقق من البيانات
- ✅ رسالة خصوصية

**المكونات**:
- `JobApplicationForm.tsx` - نموذج التقديم الكامل
- `JobImageUpload.tsx` - رفع الصور

---

### ب. لوحة التحكم الإدارية (Admin Panel)

#### 1. صفحة إدارة الوظائف `/app_admin/jobs`
**المسار**: `src/app/[locale]/(dynamic-pages)/(authenticated-pages)/app_admin/(admin-pages)/jobs/page.tsx`

**الميزات**:
- ✅ عرض جميع الوظائف (جدول)
- ✅ فلترة حسب الحالة
- ✅ البحث
- ✅ تعديل الوظائف
- ✅ حذف الوظائف
- ✅ عرض الإحصائيات

**المكونات**:
- `JobsTable.tsx` - جدول الوظائف

#### 2. صفحة إنشاء وظيفة جديدة `/app_admin/jobs/new`
**المسار**: `src/app/[locale]/(dynamic-pages)/(authenticated-pages)/app_admin/(admin-pages)/jobs/new/page.tsx`

**الميزات**:
- ✅ نموذج إنشاء وظيفة كامل
- ✅ جميع الحقول المطلوبة
- ✅ رفع صورة الوظيفة
- ✅ إضافة متطلبات ومسؤوليات ومزايا
- ✅ اختيار الحالة (نشط/مسودة/مغلق)

**المكونات**:
- `JobForm.tsx` - نموذج الوظيفة
- `JobImageUpload.tsx` - رفع الصورة

#### 3. صفحة إدارة التقديمات `/app_admin/job-applications`
**المسار**: `src/app/[locale]/(dynamic-pages)/(authenticated-pages)/app_admin/(admin-pages)/job-applications/`

**الميزات**:
- ✅ عرض جميع التقديمات
- ✅ فلترة حسب:
  - الوظيفة
  - الحالة
  - المحافظة
  - التاريخ
- ✅ تحديث حالة التقديم
- ✅ إضافة ملاحظات إدارية
- ✅ عرض تفاصيل المتقدم
- ✅ تحميل المرفقات

---

## 🔧 الوظائف البرمجية (Functions)

### ملف `src/data/jobs/queries.ts`

#### 1. `getActiveJobs()`
```typescript
getActiveJobs(filters?, page, limit): Promise<JobsListResponse>
```
- جلب الوظائف النشطة مع التصفية والترقيم
- دعم البحث النصي
- إرجاع الإحصائيات

#### 2. `getJobById()`
```typescript
getJobById(id): Promise<JobWithStatistics | null>
```
- جلب وظيفة واحدة بالـ ID
- إرجاع الإحصائيات

#### 3. `getAllJobs()` (للأدمن)
```typescript
getAllJobs(filters?, page, limit): Promise<JobsListResponse>
```
- جلب جميع الوظائف (بما فيها المسودات والمغلقة)

#### 4. `getJobApplications()` (للأدمن)
```typescript
getJobApplications(filters?, page, limit): Promise<ApplicationsListResponse>
```
- جلب جميع التقديمات مع التصفية

#### 5. `getJobApplicationById()` (للأدمن)
```typescript
getJobApplicationById(id): Promise<JobApplicationWithJob | null>
```
- جلب تقديم واحد مع معلومات الوظيفة

---

### ملف `src/data/jobs/actions.ts`

#### 1. `createJob()` (للأدمن)
```typescript
createJob(input: CreateJobInput): Promise<ActionResult>
```
- إنشاء وظيفة جديدة
- التحقق من الصلاحيات
- Revalidate الصفحات

#### 2. `updateJob()` (للأدمن)
```typescript
updateJob(input: UpdateJobInput): Promise<ActionResult>
```
- تحديث وظيفة موجودة
- Revalidate الصفحات

#### 3. `deleteJob()` (للأدمن)
```typescript
deleteJob(id: string): Promise<ActionResult>
```
- حذف وظيفة
- Revalidate الصفحات

#### 4. `submitJobApplication()`
```typescript
submitJobApplication(input: CreateJobApplicationInput): Promise<ActionResult>
```
- تقديم طلب توظيف
- التحقق من البيانات
- إنشاء سجل في job_applications

#### 5. `updateJobApplicationStatus()` (للأدمن)
```typescript
updateJobApplicationStatus(input: UpdateJobApplicationInput): Promise<ActionResult>
```
- تحديث حالة التقديم
- إضافة ملاحظات إدارية

#### 6. `incrementJobViews()`
```typescript
incrementJobViews(jobId: string): Promise<void>
```
- زيادة عداد المشاهدات

---

## 📋 التصنيفات والأنواع

### 1. تصنيفات الوظائف (Job Categories)
```typescript
- data-entry       // إدخال بيانات
- management       // إدارة
- public-relations // علاقات عامة
- secretary        // سكرتارية
- marketing        // تسويق
- technical        // تقني
- customer-service // خدمة عملاء
- other            // أخرى
```

### 2. أنواع التوظيف (Employment Types)
```typescript
- full-time   // دوام كامل
- part-time   // دوام جزئي
- contract    // عقد
- internship  // تدريب
```

### 3. أماكن العمل (Work Locations)
```typescript
- office  // من المقر
- remote  // عن بُعد
- hybrid  // مختلط
```

### 4. حالات الوظيفة (Job Status)
```typescript
- active  // نشط
- closed  // مغلق
- draft   // مسودة
```

### 5. حالات التقديم (Application Status)
```typescript
- pending      // قيد الانتظار
- reviewing    // قيد المراجعة
- shortlisted  // القائمة المختصرة
- rejected     // مرفوض
- accepted     // مقبول
```

### 6. المستويات التعليمية (Education Levels)
```typescript
- high-school  // ثانوية عامة
- diploma      // دبلوم
- bachelor     // بكالوريوس
- master       // ماجستير
- phd          // دكتوراه
- other        // أخرى
```

---

## ✅ الميزات المكتملة

### 1. إدارة الوظائف ✅
- [x] إنشاء وظيفة جديدة
- [x] تعديل وظيفة موجودة
- [x] حذف وظيفة
- [x] تغيير حالة الوظيفة (نشط/مغلق/مسودة)
- [x] رفع صورة للوظيفة
- [x] إضافة متطلبات ومسؤوليات ومزايا

### 2. عرض الوظائف ✅
- [x] قائمة الوظائف النشطة
- [x] تفاصيل الوظيفة الكاملة
- [x] تصفية متقدمة
- [x] بحث نصي
- [x] ترقيم الصفحات
- [x] عرض الإحصائيات

### 3. التقديم على الوظائف ✅
- [x] نموذج تقديم شامل
- [x] رفع السيرة الذاتية
- [x] خطاب التقديم
- [x] رابط الأعمال
- [x] مستندات إضافية
- [x] التحقق من البيانات

### 4. إدارة التقديمات ✅
- [x] عرض جميع التقديمات
- [x] فلترة التقديمات
- [x] تحديث حالة التقديم
- [x] إضافة ملاحظات إدارية
- [x] عرض تفاصيل المتقدم
- [x] تحميل المرفقات

### 5. الإحصائيات ✅
- [x] عدد المشاهدات لكل وظيفة
- [x] عدد التقديمات لكل وظيفة
- [x] تحديث تلقائي للإحصائيات

---

## 🚀 الميزات المتقدمة

### 1. نظام التصفية
- ✅ تصفية حسب التصنيف
- ✅ تصفية حسب نوع العمل
- ✅ تصفية حسب نوع التوظيف
- ✅ تصفية حسب المحافظة
- ✅ بحث نصي في العنوان والوصف

### 2. نظام الإحصائيات
- ✅ تتبع المشاهدات
- ✅ تتبع التقديمات
- ✅ عرض الإحصائيات في لوحة التحكم

### 3. رفع الملفات
- ✅ رفع صورة الوظيفة
- ✅ رفع السيرة الذاتية (CV)
- ✅ رفع مستندات إضافية
- ✅ دعم أنواع ملفات متعددة

### 4. التحقق والأمان
- ✅ التحقق من الصلاحيات (Admin only)
- ✅ التحقق من البيانات المدخلة
- ✅ حماية البيانات الشخصية
- ✅ رسائل خصوصية

---

## 📊 إحصائيات النظام

### الملفات
- **Types**: 1 ملف (354 سطر)
- **Queries**: 1 ملف (~250 سطر)
- **Actions**: 1 ملف (~250 سطر)
- **Pages**: 5 صفحات
- **Components**: 4 مكونات

### الوظائف البرمجية
- **Queries**: 5 دوال
- **Actions**: 6 دوال
- **Types**: 20+ نوع
- **Constants**: 7 قوائم ثابتة

### الصفحات
- **Public**: 3 صفحات
- **Admin**: 3 صفحات
- **Total**: 6 صفحات

---

## 🎯 حالة النظام

### ✅ مكتمل وجاهز
- نظام الوظائف **مكتمل 100%**
- جميع الميزات الأساسية **موجودة وتعمل**
- الواجهات **جاهزة ومصممة**
- قاعدة البيانات **منظمة ومهيكلة**

### ✅ الميزات الموجودة
1. إدارة الوظائف (CRUD)
2. عرض الوظائف للجمهور
3. نظام التقديم الكامل
4. إدارة التقديمات
5. نظام التصفية والبحث
6. الإحصائيات
7. رفع الملفات

---

## 💡 التحسينات المقترحة (اختيارية)

### 1. إشعارات البريد الإلكتروني
- إرسال إشعار للمتقدم عند تقديم الطلب
- إرسال إشعار عند تغيير حالة الطلب
- إشعار للأدمن عند تقديم جديد

### 2. لوحة تحكم المتقدم
- صفحة لعرض التقديمات السابقة
- تتبع حالة التقديمات
- تعديل التقديمات

### 3. تقييم المتقدمين
- نظام تقييم داخلي
- ملاحظات مفصلة
- مقابلات

### 4. تصدير البيانات
- تصدير التقديمات إلى Excel
- تصدير تقارير الإحصائيات
- طباعة السير الذاتية

### 5. تكامل خارجي
- ربط مع LinkedIn
- ربط مع Indeed
- نشر تلقائي للوظائف

---

## 📝 ملاحظات مهمة

### 1. الصلاحيات
- إنشاء وتعديل وحذف الوظائف: **Admin فقط**
- عرض الوظائف: **الجميع**
- التقديم على الوظائف: **الجميع**
- إدارة التقديمات: **Admin فقط**

### 2. الحالات
- الوظائف **النشطة** فقط تظهر للجمهور
- المسودات والمغلقة تظهر للأدمن فقط
- التقديمات تبدأ بحالة "pending"

### 3. الإحصائيات
- المشاهدات تزداد عند فتح صفحة التفاصيل
- التقديمات تزداد عند إرسال نموذج التقديم
- الإحصائيات تُحدث تلقائياً

---

## 🎉 الخلاصة

نظام الوظائف في منصة نائبك هو **نظام متكامل وجاهز للاستخدام** يشمل:

✅ **جميع الميزات الأساسية**
✅ **واجهات احترافية**
✅ **قاعدة بيانات منظمة**
✅ **كود نظيف وموثق**
✅ **أمان وحماية البيانات**

**الحالة النهائية**: 🟢 **جاهز للإنتاج**

---

**تم إعداد التقرير بواسطة**: Manus AI  
**التاريخ**: 2 نوفمبر 2025  
**المشروع**: naebak-xx
